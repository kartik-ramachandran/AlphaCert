<targets>
	<target xsi:type="Console" name="console"
	  layout="${longdate}|${level}|${message}" />
	<target xsi:type="File" name="ErrorLog" fileName="${basedir}/logs/error.txt"
			layout="${longdate}
          Trace: ${stacktrace} 
          ${message}" />
</targets>